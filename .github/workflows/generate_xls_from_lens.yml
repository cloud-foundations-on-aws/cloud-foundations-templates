name: Generate XLS Workbook from CFA Lens

on: [push]

jobs:
  run-script:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f custom-lens/requirements.txt ]; then pip install -r custom-lens/requirements.txt; fi
        
    - name: Generate XLS from a JSON
      run: python custom-lens/json-2-xls.py -s ./cloud-foundations-accelerator-custom-lens.json -d tools/cloud-foundations-accelerator-workbook.xlsx
        
    - name: Commit and push changes
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        git add .
        git commit -m "Add XLSX file created by GitHub Action"
        git push
